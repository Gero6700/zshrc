# Configuración local (overrides de máquina) — nunca se sube
.env

# Backups que install.sh genera al sobrescribir dotfiles existentes
*.bak
*.orig

# Artefactos de descarga temporales, por si algún módulo los deja en el repo por error
*.tar.gz
*.tar.xz
*.deb
*.zip
